import 'package:flutter/material.dart';

class DestopIndex extends StatelessWidget {
  const DestopIndex({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('123'),
    );
  }
}
