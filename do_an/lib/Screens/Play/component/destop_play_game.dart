import 'package:flutter/cupertino.dart';

class DestopPlayGame extends StatelessWidget {
  const DestopPlayGame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('123'),);
  }
}
